# 接口（初步）

根据提供的文档，我来为您定义前端小程序与后端服务通信的API接口。以下是涵盖全部功能的接口设计：

## 用户认证接口
*   **POST** `/api/auth/login` - 微信一键登录/手机号注册
*   **POST** `/api/auth/logout` - 用户登出
*   **GET** `/api/auth/profile` - 获取用户基本信息

## 宠物档案管理接口
*   **GET** `/api/pets` - 获取用户所有宠物列表
*   **POST** `/api/pets` - 添加新宠物档案
*   **GET** `/api/pets/{petId}` - 获取指定宠物详细信息
*   **PUT** `/api/pets/{petId}` - 编辑宠物信息
*   **DELETE** `/api/pets/{petId}` - 删除宠物档案
*   **POST** `/api/pets/{petId}/avatar` - 上传宠物头像/照片

## 健康管理接口
### 疫苗接种记录
*   **GET** `/api/pets/{petId}/vaccinations` - 获取疫苗接种记录列表
*   **POST** `/api/pets/{petId}/vaccinations` - 添加疫苗接种记录
*   **PUT** `/api/vaccinations/{recordId}` - 修改疫苗接种记录
*   **DELETE** `/api/vaccinations/{recordId}` - 删除疫苗接种记录

### 驱虫记录
*   **GET** `/api/pets/{petId}/deworming` - 获取驱虫记录列表
*   **POST** `/api/pets/{petId}/deworming` - 添加驱虫记录
*   **PUT** `/api/deworming/{recordId}` - 修改驱虫记录
*   **DELETE** `/api/deworming/{recordId}` - 删除驱虫记录

### 体检报告
*   **GET** `/api/pets/{petId}/checkups` - 获取体检记录列表
*   **POST** `/api/pets/{petId}/checkups` - 添加体检记录
*   **PUT** `/api/checkups/{recordId}` - 修改体检记录
*   **DELETE** `/api/checkups/{recordId}` - 删除体检记录

### 病历与用药记录
*   **GET** `/api/pets/{petId}/medical-records` - 获取病历记录列表
*   **POST** `/api/pets/{petId}/medical-records` - 添加病历记录
*   **PUT** `/api/medical-records/{recordId}` - 修改病历记录
*   **DELETE** `/api/medical-records/{recordId}` - 删除病历记录

### 健康数据汇总
*   **GET** `/api/pets/{petId}/health-timeline` - 获取健康数据时间线视图

## 提醒模块接口
*   **GET** `/api/reminders` - 获取用户所有提醒事项
*   **POST** `/api/reminders` - 创建自定义提醒
*   **PUT** `/api/reminders/{reminderId}` - 更新提醒状态（标记完成）
*   **DELETE** `/api/reminders/{reminderId}` - 删除提醒
*   **GET** `/api/reminders/upcoming` - 获取即将到期的提醒

## AI症状初筛接口
*   **POST** `/api/symptom-analysis` - 提交症状进行AI分析
*   **GET** `/api/symptom-analysis/history` - 获取症状分析历史记录
*   **GET** `/api/symptom-analysis/{recordId}` - 获取特定分析结果详情

## 周边服务接口
*   **GET** `/api/service-points/nearby` - 获取附近服务点（支持距离排序）
*   **GET** `/api/service-points/search` - 搜索服务点（按名称/类型）
*   **GET** `/api/service-points/{pointId}` - 获取服务点详情
*   **GET** `/api/service-points/filter` - 按条件筛选服务点（评分、类型等）

## 宠物成长时光轴接口
*   **GET** `/api/pets/{petId}/growth-timeline` - 获取宠物成长时光轴
*   **GET** `/api/pets/{petId}/growth-reports` - 获取成长报告列表
*   **POST** `/api/pets/{petId}/growth-reports` - 生成成长报告
*   **GET** `/api/pets/{petId}/growth-reports/{reportId}` - 获取具体成长报告详情

## 个性化健康计划接口
*   **GET** `/api/pets/{petId}/health-plan` - 获取个性化健康计划
*   **POST** `/api/pets/{petId}/health-plan/generate` - 重新生成健康计划

## 文件上传接口
*   **POST** `/api/upload/image` - 通用图片上传（头像、报告照片等）
*   **POST** `/api/upload/document` - 文档上传（PDF报告等）

## 系统配置接口
*   **GET** `/api/breeds` - 获取宠物品种列表（支持搜索）
*   **GET** `/api/config` - 获取系统配置信息

**接口设计说明：**
这些接口设计基于文档中的功能需求和技术架构，采用了RESTful风格，涵盖了小程序所有业务模块的前后端交互需求。每个接口都对应具体的业务功能点，为前后端开发提供了清晰的协作契约。