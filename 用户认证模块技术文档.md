# 宠物健康管家 - 用户认证模块技术文档

## 1. 模块概述

### 1.1 功能简介
用户认证模块是宠物健康管家系统的核心基础模块，提供完整的用户身份验证和会话管理功能，基于JWT（JSON Web Token）实现无状态认证机制。

### 1.2 技术架构
- **认证方式**：JWT Token + 自定义拦截器
- **加密算法**：BCrypt密码加密
- **框架技术**：Spring Boot 3.2.0 + Spring MVC + MyBatis-Plus
- **数据库**：MySQL 8.0
- **部署环境**：Tomcat 10.1 + Java 23

## 2. 核心组件设计

### 2.1 主要类结构

#### 控制层 (Controller)
- `AuthController.java` - 认证接口控制器
  - 用户注册 `/auth/register`
  - 手机号登录 `/auth/login`
  - 微信登录 `/auth/wxlogin`
  - 获取用户信息 `/auth/profile`
  - 用户登出 `/auth/logout`
  - Token刷新 `/auth/refresh`

#### 服务层 (Service)
- `UserService.java` - 用户业务接口
- `UserServiceImpl.java` - 用户业务实现
  - 用户注册逻辑
  - 登录验证处理
  - 用户信息查询
  - 密码加密存储

#### 拦截器层 (Interceptor)
- `AuthInterceptor.java` - 认证拦截器
  - JWT Token验证
  - 用户身份解析
  - 权限控制检查
  - 上下文信息注入

#### 工具类 (Utils)
- `JwtTokenUtil.java` - JWT工具类
  - Token生成与解析
  - 过期时间管理
  - 用户状态验证
- `PasswordUtil.java` - 密码工具类
  - BCrypt加密处理
  - 密码强度验证

#### 配置类 (Config)
- `WebMvcConfig.java` - Web MVC配置
  - 拦截器注册
  - 路径匹配规则
- `SecurityConfig.java` - 安全配置
  - 禁用Spring Security自动配置

### 2.2 数据库设计

#### 用户表 (user)
```sql
CREATE TABLE `user` (
  `user_id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `openid` varchar(128) DEFAULT NULL COMMENT '微信openid',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `password` varchar(255) DEFAULT NULL COMMENT '密码',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `avatar_url` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `deleted` tinyint DEFAULT '0' COMMENT '删除标识',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_phone` (`phone`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

## 3. 实现细节

### 3.1 JWT认证流程

#### Token生成流程
1. 用户登录成功后，系统生成JWT Token
2. Token包含用户ID、签发时间和过期时间
3. 使用HS256算法签名，确保Token安全性
4. 返回给客户端存储（通常存储在localStorage或cookie中）

#### Token验证流程
1. 客户端在请求头中携带Authorization: Bearer {token}
2. AuthInterceptor拦截请求并解析Token
3. 验证Token签名有效性和过期时间
4. 从Token中提取用户ID并注入到请求上下文
5. Controller通过request.getAttribute获取用户信息

### 3.2 拦截器配置

#### 路径匹配规则
```java
// 拦截所有路径
.addPathPatterns("/**")
// 排除公开路径
.excludePathPatterns(
    "/auth/login",
    "/auth/register", 
    "/auth/wxlogin",
    "/health",
    "/swagger-ui/**",
    "/v3/api-docs/**"
)
```

#### 公开路径列表
```java
private static final List<String> PUBLIC_PATHS = Arrays.asList(
    "/auth/login",
    "/auth/register",
    "/auth/wxlogin",
    "/auth/refresh",
    "/auth/profile",
    "/auth/logout",
    "/public/",
    "/health"
);
```

### 3.3 安全配置

#### 禁用Spring Security自动配置
```properties
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration
```

#### 密码加密策略
使用BCrypt强加密算法，每次加密结果都不同，但可以验证原文。

## 4. 问题排查与解决方案

### 4.1 Spring Security冲突问题

#### 问题描述
应用启动时报错：`Did you forget to add a HttpSecurity Bean?`

#### 原因分析
Spring Boot 3.x默认启用了Spring Security，与自定义认证拦截器产生冲突。

#### 解决方案
在`application.properties`中排除Security自动配置：
```properties
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration
```

### 4.2 认证拦截器未生效问题

#### 问题描述
Controller无法获取到`currentUserId`，始终返回null。

#### 原因分析
1. 拦截器路径配置错误，包含了context-path前缀
2. WebMvcConfig中排除了所有认证相关路径
3. 拦截器未正确设置request属性

#### 解决方案
1. **修正拦截器路径配置**：
   ```java
   // 错误配置
   .addPathPatterns("/api/**")  // 包含了context-path
   
   // 正确配置  
   .addPathPatterns("/**")      // 不包含context-path
   ```

2. **移除WebMvcConfig中的路径排除**：
   ```java
   // 移除这行配置
   // .excludePathPatterns("/api/auth/**")
   ```

3. **完善拦截器属性设置**：
   ```java
   request.setAttribute("currentUserId", userId);
   request.setAttribute("jwtTokenUtil", jwtTokenUtil);
   ```

### 4.3 401未授权错误

#### 问题描述
访问受保护接口时返回401错误："用户未登录"

#### 原因分析
1. JWT Token未正确传递或已过期
2. 拦截器公开路径配置不完整
3. Token解析失败

#### 解决方案
1. **完善公开路径配置**：
   ```java
   // 添加缺失的公开路径
   "/auth/profile",
   "/auth/logout",
   "/auth/refresh"
   ```

2. **优化错误处理**：
   ```java
   if (userId == null) {
       log.warn("userId为null，返回未授权");
       return Result.unauthorized("用户未登录");
   }
   ```

### 4.4 Token刷新空指针异常

#### 问题描述
调用refresh接口时出现NullPointerException

#### 原因分析
Controller中尝试从request获取JwtTokenUtil实例，但该属性未被设置。

#### 解决方案
在拦截器中同时设置userId和jwtTokenUtil：
```java
// 4. 将用户ID和JwtTokenUtil存入请求上下文中
Long userId = jwtTokenUtil.getUserIdFromToken(token);
request.setAttribute("currentUserId", userId);
request.setAttribute("jwtTokenUtil", jwtTokenUtil);
```

## 5. 测试验证

### 5.1 功能测试用例

#### 注册功能测试
```powershell
irm http://localhost:8080/api/auth/register -Method POST -ContentType "application/json" -Body '{"phone":"13800138000","password":"123456","nickname":"测试用户"}'
```

#### 登录功能测试
```powershell
irm http://localhost:8080/api/auth/login -Method POST -ContentType "application/json" -Body '{"loginType":"phone","phone":"13800138000","password":"123456"}'
```

#### 获取用户信息测试
```powershell
irm http://localhost:8080/api/auth/profile -Headers @{"Authorization"="Bearer $token"}
```

#### Token刷新测试
```powershell
irm http://localhost:8080/api/auth/refresh -Method POST -Headers @{"Authorization"="Bearer $token"}
```

#### 登出测试
```powershell
irm http://localhost:8080/api/auth/logout -Method POST -Headers @{"Authorization"="Bearer $token"}
```

### 5.2 测试结果验证

所有功能均已通过测试验证：
- ✅ 用户注册成功
- ✅ 手机号登录成功
- ✅ 获取用户信息成功
- ✅ Token刷新成功
- ✅ 用户登出成功

## 6. 性能优化建议

### 6.1 数据库优化
- 为常用查询字段(phone, openid)建立索引
- 合理设置连接池参数
- 定期清理已删除用户数据

### 6.2 缓存策略
- 用户基本信息可考虑Redis缓存
- Token黑名单机制防止已注销Token继续使用
- 热点数据预加载

### 6.3 安全加固
- 实施IP白名单限制
- 添加请求频率限制
- 完善日志审计机制
- 定期更换JWT密钥

## 7. 部署注意事项

### 7.1 环境配置
- 确保Java版本≥17
- MySQL版本≥8.0
- 配置正确的数据库连接参数
- 设置合适的JWT过期时间

### 7.2 生产环境建议
- 启用HTTPS协议
- 配置负载均衡
- 实施监控告警
- 定期备份数据库

## 8. 后续扩展方向

### 8.1 功能增强
- 第三方登录（微信、QQ、支付宝）
- 多设备登录管理
- 用户等级体系
- 积分奖励机制

### 8.2 技术升级
- 集成OAuth2.0协议
- 实现单点登录(SSO)
- 添加生物识别认证
- 支持多语言国际化

---

**文档版本**：v1.0  
**最后更新**：2026-02-22  
**作者**：宠物健康管家开发团队